# Placeholders only. Put the real file in Secrets Manager. Do not commit it.
# SYNAPSE_ENV and the database/redis URLs are overridden inside docker-compose.aws.yml.

SYNAPSE_JWT_SECRET=REPLACE-WITH-RANDOM-32-CHARS-OR-MORE
SYNAPSE_DEMO_PASSWORD=synapse-demo
SYNAPSE_GROQ_API_KEY=
SYNAPSE_OPENAI_API_KEY=
# SYNAPSE_TAVILY_API_KEY=
# SYNAPSE_STACKEXCHANGE_KEY=
